## Hystrix 的隔离策略分为线程隔离和信号量隔离

### 线程隔离

> 线程隔离时系统会创建一个默认的线程池来片Hystrix 的请求。

### 信号量隔离

> 

### 问题

- http 请求线程处理业务之后，是在什么时候切换到Hystrix 隔离线程的？

- 官网上建议在大多数情况我们应该使用线程隔离，为什么呢？

  是因为业务线程转换到Hystrix 线程之后，业务线程可以回去继续处理其它业务的原因吗？

  还是说，是因为切换到Hystrix线程之后，这个业务线程就可以不占用处理http controller 的线程资源了？

- 线程隔离策略中默认的线程池的线程数量只有10 个，这个有点太少了。但是这个太大也会消耗太多系统资源。

  为什么官方会建议使用线程隔离，仅仅是因为性能原因吗？这个性能是体现在哪个地方呢？

  如果考虑并发，那信号量应该是更好的选择吧？

- 线程隔离与信号量隔离策略可以在进程运行过程中动态切换吗？

